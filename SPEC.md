# dsh-always-status-bar

## 1. 项目目标

开发一个轻量级 DeepSeek Harness（DSH）Web UI 插件，使聊天消息原生自带的发送时间戳无需鼠标悬停即可始终显示。

插件名称：

`dsh-always-status-bar`

> 命名说明：DSH 原生将消息状态栏（用户侧的 `日期 | 时间`，Assistant 侧的
> `日期 | 时间 · 用时 · 首 token · tok/s`）渲染在同一个 span 内，插件揭示的是
> 这一整条原生状态栏，故以「status bar」命名；时间戳是其中最核心的组成。

核心原则：

> 只改变 DSH 原生时间戳的可见性，不重新生成时间戳，不改变消息结构，不修改 DSH 源码。

---

## 2. 当前行为

DSH 已经原生提供消息发送的 `日期 | 时间` 信息，但默认仅在 Hover 状态下显示。

### 用户消息

鼠标悬停到用户消息气泡后，原生时间信息出现。

### Assistant / DSH 消息

鼠标悬停到消息末尾下方的操作栏后，原生时间信息出现。

操作栏包括 DSH 原生的：

* 复制
* 点赞
* 点踩
* 其他操作按钮

以及其他插件向该区域扩展的按钮。

---

## 3. 目标行为

安装插件后：

### 用户消息

无需 Hover，原生 `日期 | 时间` 始终显示在 DSH 原本的位置。

### Assistant / DSH 消息

无需 Hover，原生 `日期 | 时间` 始终显示在消息操作栏原本的位置。

Hover 与非 Hover 状态下均保持显示。

其他操作按钮的：

* 显示逻辑
* Hover 效果
* 点击行为
* 排列方式
* 插件扩展能力

均不得受到影响。

---

## 4. 实现原则

### 4.1 必须复用 DSH 原生时间戳

不得：

* 自行读取消息时间后重新格式化
* 创建第二个时间戳组件
* 克隆原生 DOM
* 在消息旁新增自定义时间文本
* 修改日期或时间格式

必须让 **DSH 已经渲染的原生时间信息从 Hover-only 变为 Always-visible**。

因此 DSH 后续修改时间格式时，本插件应尽可能自动继承新的原生格式。

### 4.2 优先采用最小 CSS 覆盖

Agent 首先检查当前 DSH 源码，准确定位：

1. 用户消息时间戳组件及其样式；
2. Assistant 消息操作栏中的时间戳组件及其样式；
3. 控制其 Hover 显示的实际机制。

如果原生时间戳始终存在于 DOM，仅通过类似以下机制隐藏：

* `opacity`
* `visibility`
* `display`
* CSS selector
* parent hover
* group hover

则优先通过插件注入**最小范围 CSS override**，仅取消时间戳的隐藏状态。

不得为了实现该功能重写整个 Message、Bubble 或 Action Bar。

### 4.3 若并非单纯 CSS 隐藏

如果检查源码后确认原生时间戳只有进入 Hover 状态后才被 React 条件渲染，则可以通过 DSH 官方客户端插件/Slot/现有 UI 扩展机制实现。

即使采用该方案，也必须：

* 复用 DSH 已有时间数据；
* 复用原生时间显示组件或等价的官方内部能力；
* 保持原有布局；
* 不复制消息业务逻辑；
* 将改动限制在时间戳可见性这一职责内。

禁止直接修改 DeepSeek Harness 仓库源码作为最终实现方案。

---

## 5. 插件形式

插件应作为独立的 DSH 第三方插件存在，并能够通过 Web profile 安装。

遵循当前 DSH 的插件规范，包括按实际需要配置：

* `package.json`
* `dsh.bundle`
* `dsh.client`
* `cordis.patch.yml`
* Client entry
* 构建配置

具体结构以当前 checkout 中的 DSH 官方文档和已有第三方/官方 Client UI 插件为准，不要根据过期示例猜测 API。

插件只服务于浏览器端 UI，不应引入无关的 Node 后端业务。

---

## 6. 零配置要求

本插件没有任何用户设置。

禁止增加：

* Settings 页面
* 设置项
* 开关
* 配置文件
* 环境变量
* localStorage
* IndexedDB
* 数据库
* 国际化文案
* 中英文语言包
* 后端 API
* 网络请求

安装即启用。

卸载即停用。

---

## 7. UI 要求

插件不得改变 DSH 的视觉设计。

除时间戳从隐藏变成常驻以外：

* 字体不变；
* 字号不变；
* 颜色不变；
* 日期格式不变；
* 时间格式不变；
* 间距尽量不变；
* 对齐方式不变；
* 消息气泡不变；
* Assistant 操作栏不变；
* 原生按钮不变；
* 第三方插件按钮不变。

时间信息仍然位于 **DSH 原生设计的位置**。

本插件不增加任何新的可见 UI。

---

## 8. 生命周期要求

插件加载后立即生效。

应覆盖：

* 已经显示的历史消息；
* 新发送的用户消息；
* 新生成的 Assistant 消息；
* 流式生成完成后的消息；
* 切换 Session 后加载的消息；
* 加载更早历史记录后的消息。

不得依赖一次性的 DOM 扫描。

如果纯 CSS 即可完成，则不得使用：

* MutationObserver
* 定时轮询
* React DOM 手动修改
* 重复扫描 DOM

---

## 9. 卸载要求

卸载插件并重新启动 DSH 后，应完全恢复 DSH 原生行为：

* 时间戳重新仅在 Hover 时显示；
* 不留下 DOM 节点；
* 不留下 CSS；
* 不留下配置；
* 不留下本地存储数据；
* 不要求用户手动恢复设置。

插件不得修改 DSH 安装目录中的原生源码文件。

---

## 10. 兼容性要求

目标环境：

* DeepSeek Harness
* `dsh web`
* 当前项目 checkout 所对应的 DSH 版本

由于 DSH 仍在快速迭代，Agent 不得凭假设硬编码未知 DOM 结构。

开发时必须先搜索当前源码，确定真实的：

* Message renderer
* User message bubble
* Assistant message footer/action row
* Timestamp renderer
* 对应 CSS/CSS Module
* Hover selector

然后选择侵入性最低的实现。

如存在语义稳定的 `data-*` 属性、公开 Slot 或其他稳定锚点，应优先于构建产物中的随机 CSS Modules class 名。

---

## 11. 非目标

本插件不负责：

* 修改时间格式；
* 修改日期格式；
* 设置 12/24 小时制；
* 设置时区；
* 增加“显示时间”开关；
* 显示秒或毫秒；
* 给 Tool Call 单独增加时间；
* 给系统消息增加新的时间；
* 修改消息操作栏；
* 修改聊天布局；
* 保存任何用户数据；
* 提供设置页面。

---

## 12. 验收标准

开发完成后至少验证以下场景。

### A. 用户消息

进入一个已有 Session，不移动鼠标到消息气泡：

**预期：** 用户消息原生日期和时间已经可见。

### B. Assistant 消息

不移动鼠标到 Assistant 消息底部操作栏：

**预期：** 原生日期和时间已经可见。

### C. Hover

分别 Hover 用户消息和 Assistant 操作栏：

**预期：** 时间戳保持正常显示，无闪烁、跳位或重复时间。

### D. 新消息

发送一条新消息并等待 Assistant 返回：

**预期：** 双方新消息的时间戳均自动常驻显示。

### E. 历史消息

切换 Session 或加载历史记录：

**预期：** 历史消息同样自动生效。

### F. Action Bar

检查复制、点赞、点踩及其他插件添加的操作按钮：

**预期：** 功能和布局正常。

### G. 卸载

卸载 `dsh-always-status-bar` 并重新运行 DSH：

**预期：** DSH 完全恢复原生 Hover 才显示时间戳的行为。

---

## 13. 开发约束

实现时遵循以下优先级：

**原生 CSS 可见性覆盖**
→ **官方 Client UI 扩展能力**
→ **最小 React 层适配**

避免 DOM hack。

代码应保持极小规模，不为未来可能存在的需求提前设计配置系统或抽象层。

如果最终实现本质上只需要一个 Client plugin 加一小段 CSS，则保持这种简单实现，不进行过度工程化。

---

## 14. 完成定义

只有同时满足以下条件才算完成：

1. 用户消息时间始终可见；
2. Assistant 消息时间始终可见；
3. 使用原生时间信息；
4. 保持原生位置和样式；
5. 不影响消息操作按钮；
6. 无任何设置页面；
7. 无持久化数据；
8. 无后端服务；
9. 安装即生效；
10. 卸载即完全恢复；
11. 没有直接修改 DSH 源码；
12. 项目能够正常构建；
13. 可按照 README 中的本地安装方式实际加载到 `dsh web` 中验证。
